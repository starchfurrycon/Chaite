# 验证记录

## v0.7 进行中 · 范围收窄为只针对猪鲨（2026-09-23）

生产接管范围收窄为**只有猪鲨公爵（NPC type 370）**：光之女皇（NPC type 636）整体
退出范围，不再接管、不再准入、不再有逻辑与前端；明胶女士鞍（MountID 50）与光女的
扫帚（MountID 23）两条路线一并移除。程序只接管走位，不再接管武器与攻击。验收口径
改为按每套配装判定猪鲨 4 套配装的胜率（≥ 90%），不再考核无伤率。

**以下各节是按当时构建与当时范围写下的验证记录，保留原样**：其中提到的光女、
扫帚、明胶女士鞍与武器输出准入都属于那些构建，已被本节的范围变更取代，不代表
当前程序支持这些 Boss、路线或接管武器。

## v0.7 进行中：让「冲刺择时」第一次变得可搜索（2026-09-18 第四轮）

### 改动内容

向量 38 → 40 维（最后两项 = `DashReady`、`CanDash`）；冲刺头 2 路 → 3 路
（`HeadCount` 10 → 11，logit 8..10 = 不动／只按住／只强行打开）。

### 分支确实被执行：同一用例下唯一变量是头的路数

用例 `fishron/strong-wing`、右起、seed 2、专家、takeover 120、上限 12000 tick／600 s。
**同一组权重与同一阈值**（`w[4]=1.5`、`hw[9]=2.5`，即"距离 > 约 254 px 就按住"），
只有头的路数不同：

| 采样 tick 240–420（巡航期） | `plan.dash` | 观察到的 `eocDash` |
|---|---|---|
| 旧 2 路 | 几乎每行 `True` | 反复 `15`（在巡航期强行开冲刺） |
| 新 3 路 | **每行 `False`** | `0` |

而 495／540／600 等 tick 仍出现 `eocDash=15`，说明近了照样会发出。

| 运行 | status | hits | deaths | ticks | bossDamage | minLife | outcome |
|---|---|---|---|---|---|---|---|
| 基线（无策略） | win | 3 | 0 | 4632 | 78000 | 214 | SuccessNoDeath |
| 旧 2 路·距离门控 | loss | 12 | 1 | 2749 | 43573 | 0 | FailedAfterDeath |
| 新 3 路·距离门控 | loss | 13 | 1 | 2157 | 30366 | 0 | FailedAfterDeath |

**这三场里手工策略都没有赢**。本轮证明的是择时第一次**可以被表达**，不是已经找到好时机。

### 基线逐位不变（改特征前后）

| 运行 | status | hits | deaths | ticks | bossDamage | minLife | outcome |
|---|---|---|---|---|---|---|---|
| 改特征前（无策略） | win | 3 | 0 | 4632 | 78000 | 214 | SuccessNoDeath |
| 改特征后（无策略） | win | 3 | 0 | 4632 | 78000 | 214 | SuccessNoDeath |

逐位一致。无策略时残差提前返回，特征维度与头数都与它无关，这正是期望的性质。

### 变异验证（四个变异都编译通过，且对应测试确实失败）

| 变异 | 期望失败的测试 | 结果 |
|---|---|---|
| latch 还原到"提议时置位" | `HeldDashIsNotSpentUntilItIsIssued` | 失败 ✓ |
| 删掉 `&& dashProposed` 守卫 | `ForcedDashDoesNotBurnTheChargesDash` | 失败 ✓ |
| 冲刺头改回 2 路 | `DashHeadSeparatesHoldingFromForcing` | 失败 ✓ |
| 工具回退到 `$input_ = 38` | `PolicyLayoutMatchesTheToolThatWritesIt` | 失败 ✓ |

### 测试

`Chaite.Tests.exe` **758 项通过 0 失败**（新增 `PolicyLayoutMatchesTheToolThatWritesIt`、
`DashHeadSeparatesHoldingFromForcing` 两项）。旧策略文件因表头声明 38 输入而被加载器拒绝，
属 fail-closed，不是回归。

## v0.7 进行中：训练目标更正、战斗场地更正、范围收窄（2026-09-18 第二轮）

### 训练目标：改前 / 改后对照（不占机器，纯离线）

`tools/train-policy.ps1` 的排序改前为 `Clean 数量 → Hits → PlayerDamagePerK → Damage`，
`win` 不在排序里；改后为 `noHitWins → wins → deaths → hits → cleanLoopShare
→ playerDamagePerK → damage`。

把**已记录的 9 个组合**（`game-probe-rt-nh0*`，`-ScoreTag` 路径，不启动游戏）分别喂给两边：

| 组合 | 胜场 | 死亡 | 受击 | 干净闭环数 | 旧排序名次 | 新排序名次 |
| --- | --- | --- | --- | --- | --- | --- |
| `nh03` 猪鲨/高级翅膀 | **2** | 4 | 38 | 39 | **第 5** | **第 1** |
| `nh08` 光女夜/高级翅膀 | 0 | 5 | 68 | **87** | **第 1** | **第 9** |
| `nh07` 光女昼/扫帚 | 0 | 5 | 5 | 50 | 第 4 | 第 2 |

旧目标把**唯一赢过**的组合排第 5、把**从未赢过且 5 场全死**的组合排第 1。
排序不同 ⇒ 改动确实被执行，不是空转。

门禁 `tools/test-training-objective.ps1`（19 项，无游戏进程、无探针）从**源码**读出排序并与
文件头声明比对，同时回放上表的历史缺陷。**已验证它在改动前的版本上失败**，报出
`Training objective regression: ordering is Clean > Hits > PlayerDamagePerK > Damage`。

日志顺序也改了：先 `fight verdict: no-hit wins N/M (wins W, deaths D, hits H)`，
再 `loop verdict`，并把四个验收量写进 `chaite-loop-stability/v1`。

### 战斗场地：同参数探针对照

使用者指出真实场地是一条长直平地（光女约 1200 格、起点正中；猪鲨约 300 余格、
**起点贴边**、战斗时识别后镜像）。旧夹具把猪鲨出生点放在 400 格跑道**正中**，
并在平地上方加了两条贯穿全场的木平台；公布值 `platformLeft/Right=50/550` 与实际建造的
1–399 也不一致。

对照条件完全相同：`duke-fishron` / `monitor` / seed 2 / expert / `fishron-strong-wing` /
takeoverTick 120 / ticks 12000 / wallSeconds 600，两边 `config.json` 哈希相同
（`AABC490E…`）且都**不**加载策略文件（只有 `train-policy.ps1` 会设 `CHAITE_POLICY_FILE`），
因此唯一变量是夹具。

| | 旧夹具 | 新夹具 |
| --- | --- | --- |
| `platformRows` | `[460,420]` | `[]` |
| 出生 tile | 200（正中） | 21（左端内 20 格） |
| `status` | loss | **win** |
| `hits` | 8 | **4** |
| `deaths` | 1 | **0** |
| `bossDamage` | 35,593 | **78,000（满血击杀）** |
| `ticks` | 2,226 | 4,393 |
| `minLife` | 0 | 247 |

`hits` 与 `ticks` 都变了 ⇒ 改动确实被执行。且 `hits=4` 且零死亡是**本项目至今最好的单场结果**
（此前最好为 `nh03` seed 6 的 `hits=5`、零死亡）。

**结论：此前所有基于旧夹具的路线结论都需要在新夹具上重测。** 这不影响"受击来源"的结论——
45 场旧实测里**环境伤害为 0**，受击全部来自 BOSS 本体或其射弹；场地错在几何与起点，不在伤害来源。

### 范围收窄（使用者定案）

- **虾松露（坐骑 12 / `EmpressRainFishron`）不在范围内**：已从枚举、`FormulaRouteCatalog`
  三处、`CombatPlanner` 分支与 `Reset`、`EmpressFlightScript` 守卫与雨天检查、
  `FormulaMobilityContract` 雨天检查与坐骑映射、探针雨天置位与坐骑物品、三份工具门禁、
  四处契约测试中整体删除。坐骑 12 现选中 `None`（拒绝，而非驱动）。
- **冲刺无敌帧是合法资源**：此前"冲刺不利用无敌帧穿过射弹与本体"的前提作废。
  验收口径仍为 `hits`（生命下降帧）。**白天光女不受影响**：其接触与即死弹幕属
  无敌帧 Group 2，冲刺类无敌帧只给 Group 5，物理上挡不住。

### 本轮离线验证

`Chaite.Tests.exe` **754 项通过 0 失败**；`test-priority-phase-fixture-contract`（8 场景 76 元组）、
`test-priority-organic-fixture-contract`、`test-priority-boss-probe-schema`（8 场景 86 元组 8 反例）、
`test-training-objective`（19 项）全部 `exit 0`。

## v0.7 进行中：猪鲨／光女生产白名单、原生机动与战中恢复（已被上方 2026-09-23 范围变更取代）

本轮生产边界已明确固定为猪鲨公爵（NPC type 370）和光之女皇（NPC type 636，昼／夜由原生状态区分）。松露虫钓鱼、七彩草蛉释放和已存在的这两个 Boss 是唯一生产入口；其他 Boss、混合 Boss、自然排程和旧召唤链在消耗物品或发出控制输入前拒绝，并使用 `AudioCue.UnsupportedBoss`（本地槽位 `boss_too_hard_for_me.wav`）。下文较早的全 Boss 数字是历史离线回归，不能视为当前生产支持或胜率证据。

飞行／Queen 初版冻结点的 277 项离线 C# 回归、166 项 Boss 证据回归、44 项逐格统计回归、56 项普通跳证据回归、107 项飞行证据门禁回归，以及 253 项原版 API 检查（219 字面绑定、当时的四个生产钩子）通过。新增 Queen 边界用例曾因初始相对位置不符合预期而失败，已修正测试几何并保留前置断言和原行为断言；失败日志仍保留。这些结果不是 Boss 通关，也不能当作其后未发布工作树的测试总数。

此冻结点之后，工作树增加了 `HorizontalMovement` 前的可选机动边沿复核，生产注入现为五个入口；普通抓钩的读取器、纯运动、路线证书和控制器也已进入离线套件，但没有生产 Boss 路径调用该控制器。坐骑目录覆盖 66 个 `MountID`、27 个矿车标记和 64 个非零召唤物品映射，只有女巫扫帚声明受限纯运动证据；任何坐骑都尚未获得生产 Boss 驾驶授权。最低配置的模型已经拆成“Boss 所需能力门槛”和“可独立满足门槛的完整控制路线”：装备身份仅作原生参数证书，不是指定配装；路线会话锁存禁止中途跨来源拼能力。当前完整测试总数应只取最终统一构建的输出，不能把各阶段数字相加。

### 未冻结工作树：输出路线与战中接管合同

当前源码的 `WeaponProfileCatalog.Count` 为 885；`MeleeProjectileCatalog` 另有 15 个精确无弹药实验画像，源码审查记录合计 900 个。后 15 个仅能通过底层目录／求解合同，生产 Boss 输出准入会因缺少认证的闭环命中范围和原生攻击节奏而拒绝，热栏自动排名也不会让它们压过正分生产武器；它们不计入生产可用输出。Rocket Launcher＋Rocket I 通过单独的 `RocketProductionCatalog` 进入生产开火链，不计入 885 个主目录组合；其余火箭组合失败关闭。Diamond Staff 只在原版有效身体装备读取已知且没有 Gem Robe 动态特征时放行，其余六种宝石法杖没有接入。相应测试源覆盖身份、弹体、资源、时序、弹道、漂移拒绝及生产拒绝边界，但这些是离线合同，不是各画像或专用 Rocket 路线在真实客户端中的逐发试射。

F8 面对已存在的猪鲨或光女时，生产入口会优先采用当前战斗而非快捷栏召唤物。若玩家正骑乘或挂钩，入口保持原版输入并进入最多 300 tick／约 5 秒的只观察窗口；解除后重新构造快照并调用生产准入包装，Boss 离场或观察超时则取消而不接管。正式准入仍要求现有 Boss 集合、输出路线和完整机动路线全部通过，并从 `RecoverToPattern` 开始。其他 Boss 的活动根、混战或阶段证据会在这一层直接触发拒绝音频。它只证明给定快照下的阶段路由和收敛合同，不证明任意战斗位置都能接管，更不是完整 Boss 战胜场。

当前产品目标只针对猪鲨，以及光女昼／夜变种在声明最低条件下的机械化闭环。现有证据没有达到或证明“接近必胜”：`evaluate-boss-readiness.ps1` 仍对每个预声明格采用统一的至少 40 个独立种子与双侧 95% Wilson 下界 90% 门禁，不能把离线阶段回归转换成真实胜率。最终统一构建与新原版战斗批次完成前，本节不新增胜率数字。

飞行模型严格区分普通／云跳、Demon 翼推进、火箭资源转换、耗尽滑翔和真实落地恢复。`native-flight-queen01` 八个预声明轨迹全部通过，每组 600 帧，共 4800 原生帧、21136 个快照、968 次真实 WingMovement 调用。无相应调用时翼观察字段为 null，不伪造快照。

| 输入序列 | 仅恶魔之翼：最大上升 / 翼耗尽 tick | 恶魔之翼＋闪电靴：最大上升 / 翼耗尽 tick |
|---|---|---|
| 长按耗尽后松键 | 857.402832 px / 135 | 1173.03857 px / 177 |
| 早期松开再重按 | 684.343262 px / 155 | 999.979 px / 197 |
| 耗尽后保持至落地 | 857.402832 px / 135 | 1173.03857 px / 177 |
| 加云瓶、松开重按云跳 | 866.222656 px / 137 | 1181.8584 px / 179 |

独立 x86 oracle 同时比较每帧真实入口后的局部转移，以及只初始化一次、不用中途原生状态重置的完整 600 tick 自递推。纵向位置最大误差全部为 0；两组早期重按的局部速度误差最大 2.38418579×10⁻⁷、自递推最大 4.76837158×10⁻⁷ 像素／帧，其余六组速度误差为 0。预先设定的速度 10⁻⁵、位置 0.002 px 容差未放宽。

每例随机流、难度、帧计数、源及二进制哈希、进程退出和私有桌面链均经独立只读审计。玩家全程 Idle、400 HP，无 Boss 和伤害；每例可有最多一个自然敌怪，并非人为保证零小怪。没有切换用户桌面或读写用户角色／世界，隔离目录仍会产生日志与本地成就文件。

保持至第 580 帧再松键，是为了增加落地覆盖裕量：原生下落上限实为 10.01，旧 560 在带靴组仍能覆盖一个已稳定着地帧，不能写成已证实的旧时序缺陷。当前只校准固定干燥正重力竖直飞行，不覆盖完整横向／复杂地形、任意翼、火箭批次起始状态、坐骑、钩爪、羽落、倒重力飞行或端到端游戏适配器。

本批冻结输入 SHA256：

- Plugin：`E152B5F5D9566E652D25B62243090E1146BF477B81D796854655918BE083A91D`
- Core：`BD7D8EB9EEC3205D545868D5B2F0F33141E7EA4C3C27E6B4A51AD83EB2B10AF7`
- Probe：`45AC211D1C385396204DE680640C2BA41F15DB4762EC7DBDEF61E3F1281898A4`
- ProbePatcher：`0D359A8105FF11EB4FD0FD3C10A088BBE552710EAF758BB1CD338DE30984484D`

### 飞行／Queen 初版后的十病例对照

`boss-validation-flight-queen01` 使用上述冻结 Plugin／Core，以及与飞行批相同的 Probe／Patcher 源，原样重跑 v0.6 的十个开发病例。10 例均为有效原生战斗，没有拒绝、超时、夹具错误或无效证据；结果为 6 胜、4 败。这个总数只描述批次，不能用 6 场胜利抵消任一 Boss 的失败。

| Boss | 难度 | 结果 | tick | 生命下降帧 / 最低生命 | 根部观察伤害或说明 |
|---|---|---|---:|---:|---|
| 史莱姆王 | 经典 | 胜 | 3119 | 2 / 368 | 2000 |
| 史莱姆王 | 专家 | 胜 | 3975 | 7 / 189 | 2800 |
| 史莱姆王 | 大师 | 胜 | 5262 | 11 / 290 | 3570 |
| 克苏鲁之眼 | 经典 | 胜 | 3123 | 0 / 400 | 2800 |
| 克苏鲁之眼 | 专家 | 胜 | 5849 | 16 / 218 | 3640 |
| 克苏鲁之眼 | 大师 | 胜 | 7516 | 10 / 238 | 4641 |
| 史莱姆皇后 | 经典 | 失败：未击杀即脱战 | 1535 | 0 / 400 | 仅 991；不是无伤通关 |
| 毁灭者 | 经典 | 死亡失败 | 10827 | 36 / 0 | 15193；末击文本为 The Destroyer |
| 双子魔眼 | 经典 | 死亡失败 | 4445 | 66 / 0 | 16132；末击 Eye Fire |
| 机械骷髅王 | 经典 | 死亡失败 | 10858 | 27 / 0 | 头部 1432；末击 Bomb |

前六例的 tick 和生命下降帧与 v0.6 `boss-validation-jump-eye01` 完全相同，不能借飞行微测宣称它们进一步改善。史后玩家始终在地面向右移动，Boss 在 1200 tick 尚余 17087 HP，1260 tick 前已消失；这是跑出持续交战范围后的真实策略回归，不能包装为“零受击”。双子则从 v0.6 的 12201 tick 通关（根部合计伤害 43000）退化为 4445 tick 死亡；死前仍有翼资源，不能归因于单纯续航耗尽。新 Prime 样例活得更久不等于有效输出更高，头部统计也不含四只手臂伤害。

每例生产 snapshot＋plan＋capture 的 p95 为 0.1646–0.6117 ms，p99 为 0.7575–2.1345 ms，单次最大 26.3991–49.2706 ms，共 51375 个生产计时样本／56509 原生帧。这仍不包含整个游戏端到端延迟。史后因提前脱战而样本较短，其较低 p95 不能称性能改善；前六例尾延迟也有涨有跌。

逐例的装备、场地、原生难度、世界种子、独立随机指纹及逐帧随机推进、源／二进制哈希、宿主／子进程退出和私有桌面链均独立核对。输入桌面未切换，用户角色／世界未加载；隔离目录仍有测试日志和本地成就文件。失败证据完整保留，正在开发的 Queen 距离控制、Prime 原生阶段路线及后续 Twins 修复属于下一构建，不能回填改变本表。

史后按整数半血及真实攻击字段识别形态，默认低空跑道、预先横向离开下砸、依据实际平台回接；Eye 修正未来匀速窗口不能多算一个减速 tick。新构建的逐 Boss 对照尚待完成，以下 v0.6 记录仅属于历史构建。

新离线密集场景“精确 Demon tick＋Queen＋200 弹幕”384 个暖态样本的规划 p95 为 2.762 ms、p99 3.205 ms、均值 2.125 ms；只计 Core，不能当作游戏端到端延迟。只读 Reader 10,000 次热调用零分配，整个规划器仍有少量分配。

## v0.6 开发快照：原生跳跃配对与克眼专用循环

当前冻结构建通过 232 项 C# 回归、166 项 Boss 证据检查、44 项逐格统计合成回归、56 项运动证据合成回归，以及 247 项原版 API 检查（206 个字面绑定、四个生产钩子）。这些均不是 Boss 胜率。

`native-motion-jump-eye04` 的六组无 Boss 原生测试全部通过，每组 180 帧、合计 1080 帧。保留 20 帧初始松键，再分别测试长按、单帧短按、松键重按；两种装备为无饰品、只装备无前缀云瓶。原版跳跃计数、释放资格、云跳消耗、纵向速度和位置与冻结 x86 模型逐帧配对，六组最大纵向速度／位置误差均为 **0**。

| 输入序列 | 无云瓶最高上升 | 有云瓶最高上升 | 实际云跳消耗 |
|---|---|---|---|
| 长按 | 98.06738 px | 98.06738 px | 无 |
| 单帧短按 | 28.9194336 px | 28.9194336 px | 无 |
| 松键重按 | 47.3588867 px | 106.887207 px | 仅有云瓶组，第 27 帧 |

这是受限普通跳／云跳轨迹的证据，不是全部装备、复杂地形、端到端延迟或 Boss 通关保证。它验证直接原生跳跃前后与整帧纵向轨迹；横向组合另有代码回归，但尚未在本组原生测试中移动验证。翅膀、火箭、其他二跳、坐骑、液体、钩爪和同帧换装仍需独立模型与原生测试。

过程中的失败完整保留：`01` 是测试工具调用不可公开访问方法导致编译失败，未启动游戏；`02` 的原生长按轨迹有效，但原生帧计数期望错误，并揭示首次落地速度保留当帧允许位移、下一帧才归零；`03` 的短按轨迹有效，但旧日志 G7 浮点格式丢失一位 Single 精度，无法严格重建落地位置。已分别修正测试门禁、模型和日志往返格式，未通过放宽误差或删掉失败样本处理。

六例的文件清单、源／二进制哈希、原生难度、冷随机状态、独立逐帧种子递推、场景刷新、退出码和私有桌面安全链均已核对。没有切换输入桌面、发送系统键鼠或读写用户存档；隔离副本仍可能写本地成就与测试日志。运动测试包含诊断开销，不据其执行耗时宣称生产低延迟。

本次冻结输入 SHA256：

- Plugin：`F72DCB40579534E15A562126FA5303775051931A9F6572FC575AE2680C35D974`
- Core：`D9113CF3474486F8E3577CEE3C9CFDC75C97F6740CCD5E161894D283A3D61E9A`
- Probe：`16D03392D8F55CB1DA3979572701C1DCD1E1B4232FC919BCE80D4DCA7221A8CF`
- ProbePatcher：`10BB49145F0EA42BFA3BC2C13D861EED30626009C899A61A6CD19D657E2D084A`

### 相同构建的十病例开发对照

`boss-validation-jump-eye01` 完成与 v0.5 完全相同的十个 Boss／难度／种子／装备场地病例。十例均为有效原生战斗，没有测试环境错误、缺证据或超时；三项失败均是实际死亡。每格只有一个开发种子，未达到独立高可靠验收，历史构建的样本不能并入当前版本。

| Boss | 经典 | 专家 | 大师 |
|---|---|---|---|
| 史莱姆王 | 胜，3119 tick | 胜，3975 tick | 胜，5262 tick |
| 克苏鲁之眼 | 胜，3123 tick，生命未下降 | 胜，5849 tick | 胜，7516 tick |
| 史莱姆皇后 | 死亡失败，7789 tick | 本构建未测 | 本构建未测 |
| 毁灭者 | 死亡失败，8241 tick | 本构建未测 | 本构建未测 |
| 双子魔眼 | 胜，12201 tick | 本构建未测 | 本构建未测 |
| 机械骷髅王 | 死亡失败，4908 tick | 本构建未测 | 本构建未测 |

经典／专家／大师种子仍为 73492157／80528119／93641723；没有换装备、加属性、补充物品、修改 Boss AI 或重新挑选成功种子。经典克眼的生命下降帧由旧 8 降至 0，大师克眼由旧死亡失败变为本次通关，最低生命 238。但专家克眼用时从旧 2967 增至 5849 tick，本次生命下降 16 帧；不能称所有难度的输出和生存指标全面改善。史王大师本次生命下降 11 帧，也不是全部无伤。

四个肉后样例的胜负和 tick 与 v0.5 一致。双子根生命伤害 43000 包含两眼，但生命下降 84 帧、最低 81 HP，余量有限；史后、毁灭者、机械骷髅王分别只有 14703、12349、426 根伤害后死亡，必须继续修复。生命下降帧包含持续伤害，不等于独立碰撞事件数。死亡后 Boss 消失或剩余生命为零不算胜利。

逐例生产快照＋规划＋输入捕获 p95 为 0.1313–0.8988 ms，p99 为 0.2349–2.8363 ms；最大值仍有明显尾延迟，不能宣称所有帧低于 1 ms。这不包含最初适配器建立、原版更新、渲染或输入呈现。运动模型只扩大了明确普通跳／云跳子集，肉后翅膀／火箭／滑翔仍有已识别但未实现的机制缺口；下一轮会先校准这些机制与史后专用状态机。

## v0.5.0-alpha：显式武器组合与史莱姆王循环

**尚未达到“每个 Boss 独立高成功率”的目标，也未完成全部武器／弹药适配。** 194 项 C# 回归、166 项原生证据检查、44 项逐格统计合成回归通过；193 项原版 API 检查（144 个字面绑定）及四个生产钩子通过。统计测试验证验收工具，不是 44 场 Boss 胜利；新武器画像及求解器暖态 10,000 次循环零分配，也不代表整个游戏端到端零分配或延迟。

`boss-validation-weapon-king01` 使用冻结新代码完成 10 场有效原版引擎开发回归。没有测试环境错误、拒绝、缺证据或超时，失败均为实际死亡。每格仍只有一场；相同调试种子的配对观察不是独立高成功率证明。

| Boss | 经典 | 专家 | 大师 |
|---|---|---|---|
| 史莱姆王 | 胜，3064 tick | 胜，4127 tick | 胜，5244 tick |
| 克苏鲁之眼 | 胜，3336 tick | 胜，2967 tick | 死亡失败，5204 tick |
| 史莱姆皇后 | 死亡失败，7789 tick | 本构建未测 | 本构建未测 |
| 毁灭者 | 死亡失败，8241 tick | 本构建未测 | 本构建未测 |
| 双子魔眼 | 胜，12201 tick | 本构建未测 | 本构建未测 |
| 机械骷髅王 | 死亡失败，4908 tick | 本构建未测 | 本构建未测 |

经典／专家／大师对应 seed 73492157／80528119／93641723。沿用 v0.4 的真实装备、有限弹药和人工场地；新探针只增加初始武器／弹药字段及原生弹丸日志，没有修改 Boss、玩家伤害或掉落机制。每例的原生难度、首见 Boss、独立随机流递推、全部文件哈希和隔离桌面链均核对。测试在不切换的私有桌面中执行，没有前台输入、正式安装或用户存档读写。

与 `support18-final02` 相同病例配对：大师史莱姆王由失败恢复为胜利，生命下降帧从 11 降到 7；但专家史王从 3 增到 8，击杀也更慢，不能宣称所有指标改善。普通双子魔眼首次通关，根生命伤害 43000 对应两眼初始生命之和，不是只击败一个。毁灭者则由旧版存活超时变为死亡失败，仍须修复。其他 Boss 和变种尚未有此构建的证据，不以已测试的胜利抵消它们。

这 10 例每例生产快照＋规划＋输入捕获：p95 **0.1662–1.1576 ms**、p99 **0.4834–3.5151 ms**，最大值 **28.4801–42.3376 ms**。不含首次 Facade 建立、原版更新、渲染或输入呈现；不是端到端延迟，不保证始终低于 1 ms。

冻结输入 SHA256：

- Plugin：`ADD98B8EC5DE2A7BE53130D068B4FD3DC914DD2F49A0F6AFFE6EF2688A2D3A07`
- Core：`599D324F95C80B9126322CF28D3E7B40EAB292B9933E4AD65E7A715322042326`
- Probe：`5E7B72432289B5492873799E85028CF383334A09F6EF749DA5A7A7091491061B`

### 冻结代码后的 9 场史莱姆王新种子补测

`boss-validation-king-expansion01` 与前批使用完全相同的 Plugin／Core／Probe，9 场全部有效零死亡通关，无超时或测试环境错误。事先列出三个未参与前述调试的种子，并在三难度分别运行，不根据结果换种子或选择成功重试。

| 新战斗种子 | 经典通关 tick | 专家通关 tick | 大师通关 tick |
|---|---|---|---|
| 114700291 | 2932 | 3983 | 4878 |
| 587913103 | 2904 | 3840 | 4701 |
| 1482680249 | 3024 | 3962 | 4996 |

连同前批，**史王每难度有 4 个不同种子的零死亡通过记录**，不是所有 Boss 的胜利，也不是已达到高成功率验收。每格仍未满 40 个独立样本；即使把 4/4 当成独立样本，双侧 95% Wilson 下界也仅约 51%，不能把样本 100% 当作真实胜率保证。原来的一个调试种子与新增种子的角色也明确区分，没有事后宣称它们都是未见正式验收数据。

补测中，大师 seed 587913103 出现 14 个生命下降帧、最低 78 HP，因此也不宣称无伤或所有样例均有充足安全余量。其他 Boss 的失败仍按上表保留；全 Boss、各变种、各角色能力和全部武器／弹药组合尚未完成。

9 场新增运行的生产快照＋规划＋捕获 p95 为 0.1592–0.2919 ms，p99 为 0.6227–1.3435 ms，最大值 30.0242–52.5214 ms；计时范围与前批相同，均不包含整个游戏端到端延迟。所有新种子的原生冷状态、独立种子推进、文件哈希与退出／桌面链均审计；用户正常前台切换不属于测试程序抢前台。

v0.5 仍为实验包，不覆盖旧 v0.4 回退包。下方 v0.4 记录均为旧构建，不能合并为当前版本的成功率。

## v0.4.0-alpha：多 Boss 实测，不隐去败绩

当前交付候选：159 项 C# 回归、166 项证据验收、188 项原版 API 检查及六组 UI 布局检查通过；**最新 18 场为 4 胜、13 次死亡失败、1 次战斗超时**。未达到 90%，也没有相对修复前 5/18 提高样本通关率；本版是工程实验预览，不是稳定全 Boss 自动通关产品。下文保留整个迭代过程，最新实测见“平台与有限飞行修正后的完整 18 场”。

目标仍为 Windows Steam Terraria 1.4.5.8 x86。以下开发测试只设置 Main.rand，却未设置原版命名战斗随机流所依据的 ActiveWorldFileData.Seed（默认为 0）；因此这些是经典难度的观察结果，**不是已验证的完整种子对照，也不是一般游玩胜率**。

平台支撑修复前候选 Release 构建与 137 项离线回归通过；原版只读 API 契约 185 项通过（138 个直接提取的 Facade 绑定），四个生产钩子在工作区副本中的数量、位置和顺序通过。正式游戏 SHA-256 未改变。视线缓存、落地输入、平台方向、弹药只读查询、开战安全门槛和毁灭者选靶均有生产实现回归。

| Boss | v0.3 二进制基线 | 第一轮修复 | 第二轮修复 |
|---|---|---|---|
| 克苏鲁之眼 | 胜，5572 tick | 胜，4003 tick | 胜，4003 tick |
| 史莱姆王 | 胜，4169 tick | 胜，2683 tick | 胜，2691 tick |
| 史莱姆皇后 | 环境初始化错误 | 未确认击杀便脱战 | 死亡，5630 tick |
| 毁灭者 | 死亡，2367 tick | 死亡，8354 tick | 死亡，21351 tick |
| 双子魔眼 | 死亡，3693 tick | 死亡，4188 tick | 死亡，5169 tick |
| 机械骷髅王 | 死亡，3874 tick | 死亡，7635 tick | 死亡，7635 tick |

基线总尝试 2/6 胜，有效开战 2/5；第一、第二轮均为有效开战 2/6 胜。另有一次修正 Queen 初始化后的 v0.3 单独补测：正常开战、2757 伤害后脱战，不是胜利，未覆盖或删除原来的环境错误。毁灭者第二轮实际根部伤害由基线 688 提高至 25517；延长战斗与增加伤害没有变成胜利。两种肉前 Boss 虽更快结束，生命下降次数也增加，不宣称生存性全面提高。Boss 死亡脱战后的剩余血量记录为 0 不代表击杀，以原生掉落钩子和状态机结论为准。

装备在各轮保持一致，只有基线头盔标签纠错：肉前是 400 HP、铂金套、赫尔墨斯靴/云瓶/再生饰带/镣铐/疾风脚镯、迷你鲨/火枪子弹；肉后是**精金近战头盔**、精金胸腿、闪电靴/恶魔之翼/再生饰带/黑曜石护盾/游侠徽章、发条枪/水晶子弹。均有钩爪、20 瓶对应阶段治疗药、9999 发初始弹药，不补物品或生命。专家及大师肉后额外开启恶魔之心槽并装备克盾。并非最低装备或最合适武器的证明。

场地是 2600 格灰砖长地面；肉后额外两层各 500 格木平台（地面 y=500、平台 y=460/420）。Queen 使用真实珍珠石地面，不强写环境标记。初始化先原生 Scan→UpdateBiomes；第二轮进一步补齐无渲染环境遗漏的每 tick 原生 SceneMetrics 扫描，扫描次数与原生帧数一致。因此迭代并非只有单个策略变量变化，也包含明确记录的夹具正确性修正。

第二轮 snapshot+plan+capture 的逐例 p95 为 0.152–0.458 ms，峰值 19.079–29.044 ms；不含渲染、系统输入、最初 Facade 初始化，不能宣称始终亚毫秒或无卡顿。场景扫描计入独立 nativeUpdate 计时，不混入该热路径数字。

开发证据：`artifacts/boss-validation-baseline01`、`boss-validation-iteration01`、`boss-validation-iteration02`，以及 `game-probe-batch-compilecheck03` 的 Queen 单独补测。保留每例独立 result、manifest、输入二进制/夹具哈希、原生日志与桌面宿主日志。所有测试均在工作区测试副本/独立桌面运行；正式游戏文件、用户世界与角色没有载入或写入，隔离目录会产生本地成就和日志文件。

第一次计划跨三难度的 `boss-validation-unseen18-final01` 在完成 5 例后作废并停止：Main.GameMode 在新建 ActiveWorldFileData 前设置，随后被默认经典值覆盖；Main.SwapRandom 的命名随机流又使用未设置的世界种子 0。实际专家/大师克眼仍是 2800 HP，暴露了问题。进一步核对确认 Initialize 对已存在 Main.rand 有非空保护，不应归因于无条件覆盖。该目录原始 JSON 虽含 win 标记，**不得计为跨难度或独立种子成功**，详见其 INVALIDATED.md。后续测试必须原生读取并断言实际难度、世界种子及随机流，再用新目录运行。

修正后的夹具先建立最终 WorldFileData，再调用原版 SetSeed 和 GameMode 设置；直接校验经典/专家/大师的原生 difficulty 为 1/2/3（GameMode 为 0/1/2），并记录首见 Boss 的 lifeMax 和 npc.difficulty。所有初始化结束后重建命名随机流注册表、安装 Main.rand，用未消费的独立随机数实例比对真实初始状态；指纹只读取独立实例，不消耗战斗随机流。每 tick 补做一次原版 Utils.RandomNextSeed，前后检查随机数引用与推进值。批次验收也核对这些原生结果，而不是只相信命令行标签。

这仍是**指定人工场地、原生难度及已控制战斗随机流的覆盖测试**，不是完整世界 seed 重放：未控制 WorldGen、其他线程或时间来源；headless 绕过的外层画面/环境更新也不完全等同于真实客户端。相同种子的重复观察不能冒充独立样本。

### 难度/随机流修正后的 18 场：平台支撑修复前对照

`boss-validation-unseen18-final02` 全部完成，18 次有效开战，**5 胜、13 次死亡失败（27.78%）**；无拒绝、超时、环境或证据错误。经典/专家/大师分别使用战斗流 seed 73492157/80528119/93641723，每个 Boss×难度只有一次，不能证明一般胜率，更没有达到 90%。

| Boss | 经典 | 专家 | 大师 |
|---|---|---|---|
| 克苏鲁之眼 | 胜，3580 tick | 胜，5880 tick | 败，5789 tick |
| 史莱姆王 | 胜，2769 tick | 胜，3482 tick | 胜，4415 tick |
| 史莱姆皇后 | 败，6488 tick | 败，4660 tick | 败，1859 tick |
| 毁灭者 | 败，13511 tick | 败，9173 tick | 败，2335 tick |
| 双子魔眼 | 败，5372 tick | 败，2934 tick | 败，3153 tick |
| 机械骷髅王 | 败，7967 tick | 败，3949 tick | 败，3722 tick |

逐例实际 Boss 难度、首见生命值和世界种子均有记录；另以独立整数递推核对最终 UnpausedUpdateSeed，并核对每帧引用/推进/场景刷新次数、测试文件哈希与宿主退出/非抢前台记录。所有失效批次继续保留，不混入这里。生命下降帧可能包含持续伤害；机械骷髅王 bossDamage 只统计头部，不含四条手臂伤害；战败脱战后的剩余 Boss 血量 0 不是击杀证据。

此批输入 Plugin SHA-256 为 `88DE401255E46C4ADC216726D6D6D452F3FED6830503236B289D82328289DD25`，Core 为 `459A5DC5070F62944D4796F97B581A10D65CE15E57781BDCB497F267C0533376`。它是后续平台支撑修复的**修复前对照**，不能当成新二进制已完成实战验收。

### 平台修复过程中的有效回归记录

首个支撑修复候选 `boss-validation-support18-final01` 完成 5 场后停止于第 6 场准备阶段，没有启动第 6 场游戏。前 5 场为**有效的 3 胜、2 次死亡失败**，不是作废的测试环境结果：经典克眼 3671 tick 胜、专家克眼 5055 tick 败、大师克眼 5608 tick 败、经典史王 2729 tick 胜、专家史王 3601 tick 胜。专家克眼从前述成功变为剩 228 HP 时死亡，经典克眼生命下降帧从 2 增至 18。这是实际回归，不包装为改善，也不把未执行场次计入胜负。

排查确认新回接逻辑缺少“是否真正有有限飞行能力”的判断：无翼角色的零资源被误当耗尽，普通跳跃时横向风筝意图也被置零。修正加入真实翅膀/火箭靴与最大容量检查；原版没有火箭靴时 rocketTimeMax 也默认为 7，所以不能只检查最大计时是否大于零。相关失败与 STOPPED_REGRESSION.md 永久保留，修正后用新二进制、新目录按原计划复测，而非重复同版挑成功结果。

此能力修正后的 Release 已通过 159 项 C# 回归、166 项严格证据验收检查；188 项原版 API 检查（141 个直接提取绑定）及四个生产钩子通过。新增回归涵盖真实支撑边界、跨空缺口、单向台/反重力、对角穿面、斜坡/半砖排除、有界缓存、落地强刷和普通跳跃不触发补飞。API 检查器首次不认识新 byte 返回类型而停止，补齐 System.Byte 映射后通过，没有放松游戏字段检查。

### 平台与有限飞行修正后的完整 18 场

`boss-validation-support18-final02` 按原计划全部完成：**4 胜、13 次死亡失败、1 次战斗超时，18 次有效开战**，无拒绝、环境、宿主或证据错误。固定夹具的尝试/有效开战成功比例均为 4/18（22.22%），不是一般玩家的统计胜率。装备、场地、Boss×难度与声明种子和前述完整对照保持一致，不调高装备来筛选成功结果；除已控制流之外的其他随机来源仍未证明完全确定性。

| Boss | 经典 | 专家 | 大师 |
|---|---|---|---|
| 克苏鲁之眼 | 胜，3336 tick | 胜，2696 tick | 死亡，5247 tick |
| 史莱姆王 | 胜，2698 tick | 胜，3815 tick | 死亡，4238 tick |
| 史莱姆皇后 | 死亡，6590 tick | 死亡，3346 tick | 死亡，4133 tick |
| 毁灭者 | 存活超时，21977 原生帧 | 死亡，4035 tick | 死亡，3845 tick |
| 双子魔眼 | 死亡，6242 tick | 死亡，2852 tick | 死亡，3371 tick |
| 机械骷髅王 | 死亡，6856 tick | 死亡，2010 tick | 死亡，1985 tick |

经典毁灭者在第 21978 次 BeforeUpdate 触及进程 90 秒墙钟上限，完成了 21977 个原生帧（少于 24000 帧限制）；仍处 EngagedAlive、零死亡，最后观察的 Boss 剩余 51705/80000 HP、已造成 28295 根伤害。**存活超时既不是胜利也不是死亡失败**。计时上限包含初始化，实际战斗循环约 87.54 秒；推进/引用计数按已完成原生帧核对，没有漏帧成功判定。

专家克眼从完整对照的 5880 tick 改善为 2696 tick，生命下降帧从 37 降为 13；经典克眼虽更快，生命下降帧从 2 增为 8。大师史王从获胜退化为剩 199/3570 HP 时死亡。肉后效果有升有降且没有通关：例如专家毁灭者更早失败、伤害更少，不能只挑经典毁灭者存活更久来宣称优化达标。所有这些退步保留，不作“环境错误”剔除。

此批逐例 snapshot+plan+capture 的 p95 为 **0.1349–0.5838 ms**、p99 为 0.5930–2.5186 ms，但各例最大值达 18.8367–49.9925 ms；不包含渲染、系统输入或最初 Facade 初始化，不能称为始终亚毫秒或端到端低延迟保证。局部支撑的 tile 查询计入这里，原生 SceneMetrics 则计入独立 nativeUpdate。离线高密度基准仍另测 200 弹幕及光束，不把简单战斗数字泛化到所有 Boss。

最终输入 Plugin SHA-256 为 `05AD20D983FE5B27C7649C1EDC8B2E58283A3567A128D85679DEB0F2AC4B464A`，Core 为 `1A44CC4769796199F4048F20C185D8D4748A205016AA1ACD01605EA7029925E6`，GameProbe 为 `470B50C5878F8E82DE94A9A9CB346F92009EACD10B3A304A5D2CF5751973F8C7`。本地 API 证据为 `verification-v0.4.0/api-support-final03`，C# 回归为 `support-tests02.txt`，UI 为 `ui-support-final`（六组通过，未抢前台；只是几何缩放，不是真实跨显示器切换）。正式 Terraria 文件哈希未变，用户存档未载入或写入；测试副本与失败证据均保留。

下一项有证据的瓶颈是跳跃阶段建模：原版长按延长上升，云瓶二跳要释放后重按；当前预测尚未读取完整 jump/云瓶剩余状态。需要无 Boss 的原生逐帧 tap/hold/二跳对照，再修模型与动作链。现有每 60 tick 日志不足以把单场史王败绩完全归因于它，也不支持“最低机动性、最简场地、全 Boss 必胜”的声明。

### 复现多 Boss 测试

```powershell
# 默认只打印计划，不启动游戏；-Run 才串行执行六例。
.\tools\run-boss-validation.ps1 -Suite smoke6
.\tools\run-boss-validation.ps1 -Suite smoke6 -Run
# 六类 Boss × 三个经典难度 seed；不是三种难度。
.\tools\run-boss-validation.ps1 -Suite standard18
# 自定义 JSON 可分别指定 classic/expert/master；必须是项目内绝对路径。
.\tools\run-boss-validation.ps1 -Cases "$PWD\artifacts\my-cases.json"
```

自定义 schema 为 `chaite-boss-cases/v1`，`cases` 中每项包含 `scenario`、`seed`、`difficulty`，可选 `maxTicks` 和 `wallSeconds`。每例最多 24000 原生 tick/90 秒墙钟，超时不会当成胜利。批内不要构建或更改 GameProbe/GameProbePatcher；检测到版本改变会拒绝后续启动，保留错误。

## v0.3.0-alpha 历史验证（以下不代表 v0.4 全部场景）

日期：2026-09-10。目标 Windows Steam Terraria 1.4.5.8 x86。

## 本轮结果

- Release 构建成功；107 项离线回归通过。
- 原版程序集只读 API 契约：181 项检查，其中 136 个绑定直接提取自当前 Facade；四个生产钩子的数量、位置与顺序通过，正式游戏源文件哈希未改变。
- 工作区副本执行原版 AI、角色物理、选槽、物品使用、弹幕与伤害/掉落流程。经典克苏鲁之眼自动召唤、输出、击破、结束接管全部通过：5011 个原生 tick，9 次生命下降，零死亡。**不是无伤，也不是完整图形客户端测试。**
- 月总死亡射线与光女日舞：348 个配置、55,680 次原版碰撞对照，0 漏报、445 个保守额外命中。已打印的额外样例为零面积贴边；不据此推断全部额外样例同因或完全等价。
- 管理器采用非激活、屏幕外多尺寸/缩放/长文本检查。打包必须再次通过 UI smoke，证据在对应 package-check 目录。

## 后台战斗的准确范围

测试使用本机拥有的原版程序集，仅复制到工作区：`Main.dedServ=true`、`netMode=0`，运行 `Main.DoUpdateInWorld`，不是自写 Boss 模拟器。

夹具是 4200×1200 内存世界，其中 2600 格长平坦灰砖地面；经典难度、夜间、400 HP，熔岩套、幽灵靴、云瓶、克盾、钩爪、迷你鲨、9999 发火枪子弹，第 2 个快捷栏槽放一个可疑眼球。只在初始化时配置装备，战斗中不改生命、无敌、攻击力、NPC AI 或物品消耗。不能据此证明最低机动性或最简场地。

F8/F9 由测试副本的确定性热键采样器提供，不使用操作系统输入。保留四个生产入口及真实快捷栏时序；生产 DLL 仅在测试副本中替换热键轮询，其他控制逻辑不改。必须观察到召唤消耗、角色移动、发射与 Boss 扣血；只有真实击杀钩子使状态机确认成功，且完成断言全部成立，退出码才为 0。拒绝激活、超时、异常、取消或失败都不是成功。

专用服务器初始化缺少客户端数据系统，因此探针补齐本地成就、CPU 光照与摄像机矩阵。测试副本禁止 Player/World/Map 存盘，省略 Main.NewText 的绘制、CombatText 浮动字和碎块更新，并关闭物品弹出文字。原版更新中捕获的异常被显式报告，不能吞掉异常后判成功。测试 IL 短跳转统一展开，防止探针插入导致偏移溢出。

输出强制放入工作区隔离目录；SocialMode.None，不载入用户角色、世界或地图，不向 Steam 连接。启动前校验 manifest/全部文件哈希、准确 Save 路径、空存储、参数白名单，拒绝链接目录和重复运行。独立桌面不切换为输入桌面；宿主确认测试进程从未抢前台，超时只结束自己的 Job。这不是通用网络/文件系统沙箱；隔离依据是上述显式限制。

完整 XNA 图形客户端在非活动独立桌面上因 Direct3D Reach 设备初始化失败，未换用用户桌面。dedServ 分支的近战判定尺寸与客户端贴图分支不同，因此本次枪械测试不能验证精确客户端近战；手柄锁定、真实显示器 DPI/缩放、特殊召唤和其他 Boss/变种尚未实战验收。

## 离线覆盖与延迟

覆盖 324 个 Boss/难度/阶段组合的有限输出、288 个固定场景缓存/剪枝等价性；新增旋转光束连续扫掠、96 个原版分段运动场景、解钩恢复、真实 Facade 延迟选槽和倒置瞄准检查。另有状态机、多 Boss、死亡/复活、紧急取消、特殊召唤、单物品事务和安装回滚保护。

本机 x86 Release，384 次暖态计时；仅 Core，不包含游戏采样、渲染或操作系统输入：

| 场景 | p50 | p95 | p99 |
|---|---:|---:|---:|
| 克眼 + 200 弹幕 | 1.216 ms | 1.660 ms | 1.906 ms |
| 猪鲨 + 200 弹幕 | 1.214 ms | 1.740 ms | 1.998 ms |
| 机械组合 + 200 弹幕 | 1.211 ms | 1.693 ms | 1.806 ms |
| 混合 Boss + 200 弹幕 | 1.189 ms | 1.689 ms | 1.919 ms |
| 200 弹幕 + 18 条长光束 | 2.588 ms | 3.994 ms | 4.948 ms |
| 原版分段跑速 + 200 弹幕 | 1.702 ms | 2.360 ms | 2.928 ms |

约 188–195 B/次，计时窗口无 GC；冷构造与首次密集规划 17.850 ms（包括 JIT）。机器负载影响明显，数字不是延迟保证。原版克眼探针中 snapshot+plan+capture 共 4591 次，平均 0.091 ms，最慢 24.295 ms；最慢值包括初次规划/JIT，不能只报告平均值。探针使用节流且不渲染，墙钟加速比不是帧率或端到端延迟。

## v0.7 战斗级无伤实测与前端玩梗改造（2026-09-18）

**结论：0/9 组合达成战斗级无伤。** 45 场中 2 场获胜，且两场都有受击。

用隔离原版探针（`verify-nohit.ps1`，串行，每场全新单次副本）对 9 个生产组合
（猪鲨 5 种配装 + 光女昼/夜各 2 种）逐组合跑门禁种子 2–6，读取每场 `result.json`：
- 判据：该组合每个种子都 `status=win` 且 `hits=0` 且 `deaths=0`。
- `hits` 定义（探针原文）：玩家生命下降的原版更新帧数，含环境伤害；不是伤害事件钩子。
- 结果：猪鲨五条路线 `hits` 落在 5–10，全部 `deaths=1`；光女昼间 `hits=1`（昼间为
  一击必杀，`minLife=0`）；光女夜间 `hits` 6–15。
- 唯一获胜的 2 场是猪鲨／高级翅膀 seed 5（hits=10，deaths=1）与 seed 6
  （hits=5，deaths=0，`minLife=229`）。
- 总耗时 31.7 分钟；报告 `CHAITE-NOHIT-VERIFY.txt`。

`verify-nohit.ps1` 与 `runwave.ps1` / `runprobe.ps1` 一样是仓库外的开发辅助脚本
（沿用 `train-policy.ps1` 现有的外部依赖约定），不随包分发。

**这一轮最重要的修正是口径，不是脚本。** 训练器给出的
「STABLE clean closed loops on 5 of 5 gate seeds」衡量的是**单个玩家闭环内无伤且回到
初始状态的数量**，与验收要求的「整场一次都没被打到」不是同一个量。把前者读成后者
是本项目第三次在闭环口径上出错，因此 README、前端与本文都写死两者必须分开报告。

前端（`Chaite.Manager`）：

- 前端重做为面向用户的三段式（标题／猪鲨·光女两栏／按键说明），移除全部开发者信息。
- 改用本体贴图：管理器运行时用 XNA `ContentManager` 从玩家自己的 `Content/Images/*.xnb`
  解码（该文件是 LZX 压缩，`flags=0x80`；XNA 自带解压，无需自实现 LZX），
  缓存到 `Chaite/Sprites/`。Boss 头像索引与中文名分别读自 Terraria.exe 内嵌的
  `NPCID.Sets.BossHeadTextures` 表与 `zh-Hans` 本地化资源。
- 冒烟测试新增 `PASS sprite-store` 与 `PASS vanilla-art`（对真实 Content 解码 17 张
  贴图，校验尺寸、非空白、两两互异；无游戏时 SKIP），并断言缓存就绪时每个贴图
  槽位都持有本体美术。管理器因此固定 x86；无显示设备的机器退回中性占位框。
- 新增梗语登记 `MemeVoice`，按状态轮换，**只在状态变化时推进**（不用计时器，
  否则两次布局之间的文本变化会让冒烟测试不稳定）。
- 新增梗图槽位 `Chaite/Memes/`：安装器创建目录并刷新 README，用户图片永不覆盖。
- 安装载荷契约新增 `Memes/README.txt`；`PatcherTests` 两个夹具同步更新并新增
  「用户梗图在升级后仍保留」断言。

验证：`Chaite.Tests.exe` **752 项通过、0 失败**（本机 x86 Release）；
`Chaite.Manager.exe --ui-smoke` 六种尺寸/缩放 **Failures: 0**，新增
`PASS meme-slot`（临时目录内真实读写、坏文件跳过、缩略图上限、文件名排序），
以及字形覆盖度与两两互异检查；预览窗口全程未抢前台。

未做：没有在真实客户端里听音频、看前端。本轮音频验证只覆盖代码路径与回归，
不宣称梗音频实播成功——隔离副本没有梗音频文件。

## 证据与复现

```powershell
.\tools\build.ps1 -Configuration Release
.\tests\Chaite.Tests\bin\Release\net48\Chaite.Tests.exe
.\tools\verify-api-contract.ps1 -OutputDirectory "$PWD\artifacts\verification-local01" -PatchCopy
.\tools\prepare-game-probe.ps1 -RunName game-probe-local01 -Headless
.\tools\start-isolated-test.ps1 -TargetExe "$PWD\artifacts\game-probe-local01\Terraria.exe" -OutputDirectory "$PWD\artifacts\game-probe-local01-desktop"
.\tools\build.ps1 -Configuration Release -Package
```

本机证据：`artifacts/verification-v0.3.0/`、`artifacts/game-probe-final02/` 与其 `-desktop` 目录。旧调试探针包含失败记录，不能代替最终证据；旧 v0.1/v0.2 包保留回退。原版 SHA-256：`960A03BFF6050CF7BE16DFC1A7B19E10FC2C4F8F835A6A3B135A50DD9E6BA2F3`。

GitHub 只发布自有源码、工具与文档。CI 只执行离线构建/回归；不提供、不下载、不运行 Terraria，不包含测试副本、存档或梗音频。
