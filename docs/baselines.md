# 各路线固定状态机基线（学习策略的对比基准）

> 目标要求"达到 95% Wilson 下界 ≥90% 才算通过"，并且**固定状态机的既有成果要作为基线保留并对比**。
> 因此每条路线在交给学习策略之前，都需要一个**在明确构建上、≥20（验收 ≥40）条独立种子**的基线。
> 本文件只记录**真正测过的数字**，未测的一律标注"未测"，不用估计值充数。

## 已测基线

| 路线 | 场景 | 种子数 | 胜 | 胜率 | Wilson 95% 下界 | 构建 | 备注 |
|---|---|---|---|---|---|---|---|
| `fishron-fairy-wing` | duke-fishron / expert / takeover 120 | 40 | 19 | **47.5%** | **32.9%** | 早期构建 | 目前最好的固定状态机成绩 |
| `empress-broom` | empress-night | 32 | 1 | **3.13%** | **0.55%** | `F0E80769` | 平均伤害 87,732（她的 89.5%）、平均受击 14.03、平均存活 6362 tick |
| `fishron-queen-slime` | duke-fishron | 20 | 2 | 10.0% | ≈0.6% | `a2ee327` 时期 | **旧构建，未在当前构建复测** |

`empress-broom` 的适应度（训练器同一公式 `damage/98000 − 0.5·hits/14`）在 46 条唯一种子上：
**平均 0.3885**、中位 0.3988、范围 0.2737–0.5357。这是学习策略必须越过的线。

## 未测（这是当前最大的流程缺口）

以下 5 条路线**没有**在明确构建上做过 ≥20 条种子的基线，
因此即使学习策略训出来，**也没有可比对象**：

- `fishron-strong-wing`
- `fishron-trusty-chillet`
- `fishron-trusty-chillet-ignis`
- `empress-strong-wing`
- `empress-rain-fishron`

**处理方式**：每条跑一次 20 条种子（同一构建、同一场景），把结果填进上表；
需要验收时再扩到 40 条。这件事与训练可以**并行**做——
但**必须等当前 `empress-broom-v1` 训练用的构建 `81D466CE` 不再被需要之后**，
或者用当前构建 `81D466CE` 跑（它已包含全部 8 处 hook；未被策略接管的路线行为与
`F0E80769` 相同，因为 hook 只在配置了策略文件时才生效）。
**后者更省时间，且不需要重编**：不带 `CHAITE_POLICY_FILE` 环境变量时，
所有路线走的都是原固定状态机分支。

## 对比口径（写死在验收流程里）

1. 同场景、同路线、同构建；
2. 训练种子与验收种子**不相交**（训练 1–60，验收 1001–1040）；
3. 学习策略与固定状态机**在完全相同的种子集合**上跑，逐种子配对对比；
4. 通过标准：**≥40 条独立种子、95% Wilson 下界 ≥90%**；
5. 达不到就如实记录差距，不用平均值掩盖。
