# 各路线固定状态机基线（学习策略的对比基准）

> **用户第 25 轮指示（本文件的当前权威）**：
> "关于验收和基线，直接以**无伤**为目标进行训练即可吧。"
>
> 因此：**训练目标 = 无伤**（不掉血地击败），**验收 = 是否达成无伤**；
> 原先那套"40 条种子 Wilson 下界 ≥90%"的复杂验收与逐路线基线补测**降为参考信息**，
> 不再作为主线工作量。

## 目标改为无伤后的适应度（已生效并验证）

旧适应度 `win → 1.0` 的问题：**挨了 14 下的胜利和无伤胜利得一样多的分**，
所以优化器**没有理由**去追求无伤——这与用户的目标直接冲突。已改为：

| 结果 | 适应度 |
|---|---|
| **无伤胜利** | **1.0000**（唯一最大值） |
| 胜利，挨 1 下 | 0.9800 |
| 胜利，挨 12 下 | 0.7600 |
| 胜利，挨 ≥25 下 | 0.5000（下限） |
| 失败，打掉 100%、挨 14 下 | 0.0000 |
| 失败，打掉 89.5%、挨 14 下（固定状态机均值） | **−0.0524** |

性质（已用构造数据实测）：**无伤是唯一最大值**；胜利内**挨打越多分越低**（单调）；
**任何胜利 ≥ 任何失败**。

**一处如实说明**：胜利下限 0.5 与失败上限 0.5 **相等**，即"任何胜利严格大于任何失败"在
`0 受击 且 恰好打掉 100%` 这个**零测度角点**上不成立（是相等而非反转）。
该角点在实测中不可能出现，故保留；如需严格可分，把失败项系数由 0.5 降到 0.49 即可。

## 新适应度下的基线（固定状态机，46 条唯一种子）

| | 新适应度 |
|---|---|
| 平均 | **−0.0264** |
| 中位 | −0.0568 |
| 最低 / 最高 | −0.1278 / **0.7400** |
| 胜利数 | 2 |
| **无伤胜利数** | **0** |

最好的一条是种子 29（胜利，挨 13 下 = 0.74），其次是种子 37（胜利，挨 14 下 = 0.72）。

**这个表说明了一件事**：固定状态机在 46 条种子里**一次无伤都没拿到**。
所以无伤不是"把现有成绩再推一点"，而是要学出**固定状态机从未产生过的行为**——
这也正是把它交给学习策略的理由。

---

## 旧口径参考（Wilson 验收，现已降为参考）

> 目标要求"达到 95% Wilson 下界 ≥90% 才算通过"，并且**固定状态机的既有成果要作为基线保留并对比**。
> 因此每条路线在交给学习策略之前，都需要一个**在明确构建上、≥20（验收 ≥40）条独立种子**的基线。
> 本文件只记录**真正测过的数字**，未测的一律标注"未测"，不用估计值充数。

## 已测基线

| 路线 | 场景 | 种子数 | 胜 | 胜率 | Wilson 95% 下界 | 构建 | 备注 |
|---|---|---|---|---|---|---|---|
| `fishron-fairy-wing` | duke-fishron / expert / takeover 120 | 40 | 19 | **47.5%** | **32.9%** | 早期构建 | 目前最好的固定状态机成绩 |
| `empress-broom` | empress-night | 32 | 1 | **3.13%** | **0.55%** | `F0E80769` | 平均伤害 87,732（她的 89.5%）、平均受击 14.03、平均存活 6362 tick |
| `fishron-queen-slime` | duke-fishron | 20 | 2 | 10.0% | ≈0.6% | `a2ee327` 时期 | **旧构建，未在当前构建复测** |

`empress-broom` 的适应度（训练器同一公式 `damage/98000 − 0.5·hits/14`）在 46 条唯一种子上：
**平均 0.3885**、中位 0.3988、范围 0.2737–0.5357。这是学习策略必须越过的线。

### 噪声：一个对我自己先前估计的更正

按范围（0.27–0.54）我先前估计逐种子标准差约 0.14；**实测只有 0.0603**。
范围大是因为少数离群种子，离散度其实不大。由此得到"独立取样"的均值标准误：

| 每代种子数 K | 均值标准误（独立取样） |
|---|---|
| 2 | 0.0427 |
| 4 | **0.0302** |
| 8 | 0.0213 |
| 16 | 0.0151 |

**若候选各自独立取样**，K=4 的噪声（0.030）会大于 σ=0.10 扰动带来的真实增益（约 <0.02），
"更好就接受"就会被噪声主导——这也是我先前担心的那件事。

**但训练器用的是配对取样（CRN）**：代内所有候选与父代跑**同一批种子**，
所以真正决定比较噪声的是**逐种子差值**的标准差 `sd·sqrt(2(1−ρ))`，其中 ρ 是两个相近策略
逐种子结果的相关性。相近策略的 ρ 应当很高（0.8–0.95），取 ρ≈0.9 估算：
差值标准差 ≈0.027，K=4 的均值标准误 ≈**0.013**，与信号量级相当——**勉强可用，但确实紧**。

**诚实标注**：ρ 需要两个策略才能实测，当前无法测，所以 0.013 是**推算而非实测**。
若训练若干代后父代适应度没有实质性移动、且保留种子始终为 0，
那就说明这个推算是乐观的，届时应把 K 提到 8–12 并相应减少代数再重启。

### 验收对应的适应度是多少，以及 60 代够不够

把验收口径换算成训练器看得懂的标量：胜 = 1.0，败 ≈ 固定状态机的 0.389。
要满足"≥40 条种子、95% Wilson 下界 ≥90%"，大约需要 **40 条里赢 38–40 条**，
对应适应度 ≈ **0.94**。而当前父代是 **0.164**。

也就是说 ES 要跨过 **约 0.78 的落差**。按每次被接受的扰动贡献 ~0.01–0.02 估算，
需要**几十次成功接受**；再考虑接受率，**60 代很可能不够**。

**这不是要现在改**（训练正在跑，中途改设置会作废已投入的时间），
而是要预先说清：看到适应度曲线后，若明显是"在爬但远远没到"，
正确的动作是**加大代数**（或提高 K 降低噪声以加快接受），
而不是因为"跑完了 60 代"就宣布失败。

## 未测（这是当前最大的流程缺口）

以下 5 条路线**没有**在明确构建上做过 ≥20 条种子的基线，
因此即使学习策略训出来，**也没有可比对象**：

- `fishron-strong-wing`
- `fishron-trusty-chillet`
- `fishron-trusty-chillet-ignis`
- `empress-strong-wing`
- `empress-rain-fishron`

**处理方式**：每条跑一次 20 条种子（同一构建、同一场景），把结果填进上表；
需要验收时再扩到 40 条。这件事与训练可以**并行**做——
但**必须等当前 `empress-broom-v1` 训练用的构建 `81D466CE` 不再被需要之后**，
或者用当前构建 `81D466CE` 跑（它已包含全部 8 处 hook；未被策略接管的路线行为与
`F0E80769` 相同，因为 hook 只在配置了策略文件时才生效）。
**后者更省时间，且不需要重编**：不带 `CHAITE_POLICY_FILE` 环境变量时，
所有路线走的都是原固定状态机分支。

## 对比口径（写死在验收流程里）

1. 同场景、同路线、同构建；
2. 训练种子与验收种子**不相交**（训练 1–60，验收 1001–1040）；
3. 学习策略与固定状态机**在完全相同的种子集合**上跑，逐种子配对对比；
4. 通过标准：**≥40 条独立种子、95% Wilson 下界 ≥90%**；
5. 达不到就如实记录差距，不用平均值掩盖。
